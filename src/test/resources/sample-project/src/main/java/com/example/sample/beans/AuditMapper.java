package com.example.sample.beans;

import org.apache.ibatis.annotations.Mapper;

/** MyBatis mapper: a terminal bean, injectable but with no outgoing edges. */
@Mapper
public interface AuditMapper {
}
